# `spikes/` — 试验与探测脚本

**一次性** 或 **排障用** 的小脚本（如 OAuth 授权探测、委派路径探测），**不是** 生产启动路径的一部分。

## 使用建议

- 在已激活的虚拟环境中运行；需已配置 `.larkagent/secrets/` 或 `.env`。  
- 阅读各文件顶部 docstring 了解参数与安全提示。

## 轻度 vs 重度

- **轻度**：通常可忽略本目录。  
- **重度**：在排查 **代发、委派、多 bot** 问题时使用，避免在生产环境随意执行未读过的脚本。
